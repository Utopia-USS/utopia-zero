# secrets - never committed
zero/.pat

# escalation scratch (issue body + payload built before posting to GitHub)
zero/.issue.md
zero/.issue.json

# plugin-less wizard clone (fallback mode)
zero/.wizard/
.env
*.env
!.env.example

# local machine noise
.claude/settings.local.json
.DS_Store
*.log

# IDE artifacts (operators peeking with an IDE must not pollute the repo)
.idea/
.vscode/
*.iml
